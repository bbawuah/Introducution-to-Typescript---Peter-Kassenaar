// index.ts, hierin kunnen we nu jQuery gebruiken
import * as $ from 'jquery';

$(document).ready(() => {
	$('#heading').text('Welkom bij TypeScript, jQuery en WebPack - Er is een watcher actief');
	//.. overige opdrachten...
});
